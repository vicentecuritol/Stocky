services:
  mysql:
    image: mysql:8.4
    container_name: Stocky-mysql
    environment:
      MYSQL_ROOT_PASSWORD: system
      MYSQL_DATABASE: db_stocky
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Stocky-app
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/db_stocky?createDatabaseIfNotExist=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: system
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"
      JWT_SECRET: bD4kP9mX2nQ7rT1vY6wZ3aE8cF0hJ5sL2uI9oN4xG7jK1mB6pR3tW8yA5qC0eH
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: admin123
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  mysql_data: